| 2026-08-26 | tokyo | [critic] | meals & energy | **a closure fact recorded on a VENUE never reaches the DAY that venue anchors** — the guide's own market entry said roughly half the stalls close on Sundays, and the Sunday day card still built the trip's #1-priority grazing breakfast there and called it "the day's only market stop", while a fully-trading Saturday slot sat unused. Third recurrence of the uruguay "the day plan quietly picks the optimistic one" class, now in its scheduling form. Before writing a day, read the `closed`/`hours` field of every venue it names against that day's weekday. | open |
| 2026-08-26 | tokyo | [critic] | row 6 (anchor coverage) | **the anchor's price came from the anchor's booking page, which does not publish a price** — the ¥70,000 used was the same-day CANCELLATION fee on a different page of the same site; the course is ¥77,000 tax-inclusive, so the all-in private-room figure was understated by ¥8,050 a head. A venue's `/reserve/` page states booking rules; its `/menu/` page states prices. Cite the page that carries the claim, not the page that carries the venue. | open |
| 2026-08-26 | tokyo | [critic] | row 7 (4-question venue rule) | **a venue's DISQUALIFYING rules are invisible to a research pass that only looks for price, hours and capacity** — the anchor restaurant refuses reservations from anyone declaring a food allergy and cannot serve non-seafood eaters, both stated plainly on its own about page, and the guide shipped a party-of-six booking plan and an allergy phrase card without either. "Book?" is not just a channel: for a reservation-gated anchor it includes who the venue will REFUSE. | open |
| 2026-08-26 | tokyo | [critic] | row 5 (itinerary integrity) | **a `basis: "day"` budget line whose own label scopes it to fewer days than the section's `days`** — "casual days (Fri, Sun, Mon, Tue), per day" was multiplied by 5, double-counting the Saturday whose food is already the separate anchor line. Sibling of the uruguay nights-vs-days row. If a food or transport line excludes a day, it belongs on `basis: "trip"` with the arithmetic written into the note. | open |
| 2026-08-26 | tokyo | [critic] | geography | **the orientation map inherits a recalled city centroid and nobody checks the frame against the guide's own coordinates** — `center` was the generic "Tokyo" latlong, which lands in a ward the trip never enters, and the rendered bbox excluded every eastern venue including the anchor dinner and both markets. The same coords feed the `weather` block. A guide that already holds coords for its sights can derive the map's centre and span from them; a centroid typed from memory is a guess wearing a schema field. | open |
| 2026-08-26 | tokyo | [critic] | inclement cover | **`plan_b` goes unwritten on the exact day the guide itself flags as closable** — the day anchored on a venue whose own card says it "closes for maintenance roughly monthly on irregular dates" had no alternate and no honest note. The trigger for the obligation is already sitting in the guide's prose as a ⚠. Any ⚠ about a venue closing should be read as a `plan_b` requirement on whichever day names that venue. | open |
| 2026-08-26 | tokyo | [critic] | row 3 (provenance) | **a real T0 URL attached to a claim that page never makes reads as verified to every check there is** — three venue cards carried "11 private rooms", "private rooms free of extra charge for 4+" and an operator price estimate, each cited to a page that publishes none of them; two also contradicted their cited page's actual party-size rule. Link-reachability and date-freshness both pass on a fabricated citation. Only fetching the page and looking for the sentence catches it. | open |
| 2026-08-26 | tokyo | [critic] | row 13 (design doctrine) | **`≈` typed into a `facts.json` `value` that also sets `state: "approx"` renders the marker twice** — three transit rows would have shipped as "≈¥2,470 … ≈ approx.". The rule that the marker is derived, not authored, has no mechanical check behind it, and the registry is exactly where the two spellings meet. Candidate gate: fail any fact row whose `value` contains `≈`. | open |
