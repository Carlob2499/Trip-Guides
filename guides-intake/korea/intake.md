# Guide Intake — Korea

> **Why this file is empty.** The Korea guide predates the `guides-intake/<slug>/` convention:
> it was authored, run and reflected on before the intake doc + `## Research reconciliation`
> ledger became standard, so no intake questionnaire was ever recorded for it. The file it was
> split from was created on 2026-07-30 by revision #27 purely so the reconciliation ledger (now
> `ledger.md` beside this) has its one append-only home, as the guide-author skill requires.
> **Sections 1–7 are deliberately blank rather than reconstructed** —
> back-filling a traveler intake from the finished guide would invent the very thing the
> intake exists to record. The trip's actual traveler evidence lives in
> `learnings/korea.md` (the maker's private post-mortem), the published `learnings` block in
> `src/content/guides/korea/_guide.json`, and `docs/evidence/traveler-patterns.md`.

## 1–7. Intake questionnaire

Not recorded — see the note above. Do not reconstruct.
