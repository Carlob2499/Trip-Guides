| 2026-08-28 | yamagata | [critic] | row 6 (anchor coverage) | **an operator's service NOTES carry the anchor's most decision-relevant content — including which DIRECTION a time runs in — and they sit beside the timetable rather than in it** — the anchor transfer first shipped times without the two ※/☆ notes on the same page (one departure does not connect to the northbound Shinkansen; the last bus is crowded and 増便はありません), so the guide's own "aim mid-afternoon" advice named precisely the unusable departure. Three passes later the same page's 「最終便18:21発」 exposed that the guide carried that one time on BOTH surfaces at once — last bus *in* on the transit step, last bus *out* on the return step. When the anchor is a scheduled service, read the notes column, carry it, and let its 発/着 settle direction before any timetable arithmetic. | open |
| 2026-08-28 | yamagata | [critic] | row 3 (provenance) | **a source that splits by VALIDITY RANGE produces both a phantom conflict and a wrong citation, in that order** — Pass A and Pass B fetched the same operator's winter table and its April–October table, read the constant offset as a cross-pass disagreement, and shipped "treat exact times as approximate"; the critic fix then named the right season and cited the winter page anyway, which says of itself that its information is out of date. Two rules, one check: before opening a disagreement investigation on two same-domain pages, look for a season or validity label on each table; and cite the page whose range CONTAINS the trip dates, re-deriving every figure (gaps, first/last, counts) from that page rather than the one read first. | open |
| 2026-08-28 | yamagata | [critic] | row 9 (party fit) | **the claim that answers a BINDING constraint is the one most likely to be cited to a page that never made it** — a guide whose intake bound mobility as material shipped "the best-documented step-free access of any Yamagata city sight" on two surfaces, cited to the venue's official site, which publishes nothing about accessibility on that page or its access subpage. The accessibility data existed on the prefecture's tourism site all along. A constraint-bound fact deserves the fetch-and-quote treatment reserved for prices, because a superlative about documentation is exactly the sentence a model can write without reading anything. | open |
| 2026-08-28 | yamagata | [critic] | row 3 (provenance) | **RECURRENCE (uruguay 2026-08-23, then four times inside this one run) of "the citation cannot support the claim, and nothing notices"** — one venue's `source_url` was a review site's bare homepage; a divergences card asserted a JR station's IC-card policy from a bus operator's fare table; a health panel asserted 2026 measles activity from a pharmacy how-to; a temple's fee page stood in for its opening hours. The section-level `source_url` is the vector every time: each distinct claim in a body owes either its own citation or its own ⚠, and the highest-confidence block types (divergences, safety) are the worst places to inherit one. Promotion candidate, mechanical half: no `source_url` may be a bare origin with an empty path. | open |
| 2026-08-28 | yamagata | [critic] | row 7 (4-question venue rule) | **a venue item is graded on its own fields and never against the itinerary, so both halves rot** — four of seven venues told the reader to call ahead and gave no number, a fifth shipped with neither address nor telephone, and every one of those numbers sat on a page the pass could already reach; one venue also shipped "hours aren't posted" when the prefecture's portal publishes them seasonally. The second half is worse because nothing looks broken: that fifth venue — the only shipped pick one indoor minute from the station — appeared in NO day card, while the trip's hardest day (1,015 steps, two low-mobility travellers) ended with no dinner named at all. Two candidate gates: `book == "call"` with no `phone` is a mechanical readiness failure; and shipped venues should be reconciled against the day cards, so an unplaced venue and a mealtime-shaped hole in the same guide are reported together. | open |
| 2026-08-28 | yamagata | [critic] | row 5 (itinerary integrity) / row 4 (completeness) | **RECURRENCE (2nd run, uruguay 2026-08-23) of "the budget block is assembled from section headings, not from the itinerary"** — twice in this one guide: a 4-day/3-night trip put city lodging on `basis: "day"` with `days: 4`, billing four city-hotel nights when two of the three nights were in the city and the third was a separately-priced ryokan; and the same block priced "local transport, per day" while omitting the intercity Shinkansen the guide's own Day 1 card opens on — ≈¥22,900/person, 29% of the per-person total, already verified and stated verbatim on two other surfaces. A ¥0 "flights — not researched" line is an honest blank about international air; it is not licence to drop a domestic fare the guide has already sourced. Build the budget by walking the day cards: every night, every leg between cities, every figure the guide already states. | open |
| 2026-08-28 | yamagata | [critic] | tone (vibe lens) | **RECURRENCE ×3 within one run: the research environment narrates itself to the traveler, and each clean-up grep searches for the LAST pass's wording** — "bot-gated against automated fetch" was cleared from three prose bodies; the next pass found it in `_guide.json`'s `verified` stamp; the next found "returned a blocked/403 response … in this research pass" in `entry[0].note`, "Cloudflare-gated against automated fetch" in `advisory.summary`, and "as of this research pass" in a booking-checklist note. Three phrasings, so two literal greps for `bot-gated` found none of them. The voice gate reads `body`/`why`/`crowd_tip`/`intro` and no guide-level field at all. Gate the CLASS — fetch/scrape/403/bot/"this research pass" vocabulary — across every string in the guide directory, not the phrase the last critic happened to delete. | open |
| 2026-08-28 | yamagata | [critic] | row 3 (provenance) | **the `facts.json` money registry gets used for the two facts a pass thinks of as "the figures" and skipped for the other eight** — this guide registered the bus fare and the temple admission, then typed a Shinkansen fare, a train fare, two restaurant minimums, a museum price, a taxi range and a ryokan rate straight into prose across six files, several of them appearing twice. SKILL.md is unambiguous that perishable money facts go in the registry. The registry earns its keep exactly where the duplication is, so the rule to apply is per-figure and mechanical, not "the important ones". | open |
| 2026-08-28 | yamagata | [critic] | row 7 (4-question venue rule) / row 11 (recency) | **CLOCK and CALENDAR facts get a fraction of the scrutiny MONEY facts get, and they fail by ARITHMETIC rather than by a bad source** — every price here was audited to a quoted primary source, while a 1,015-step temple day shipped on "the 8:00 opening" read off a season the trip is not in (the temple publishes 4月-9月 and 12月-3月 and **nothing for October**) with no closing time stated anywhere; an onsen arrival was timed to a lamp window its citation gives only as "at dusk"; and the trip's one time-critical booking was dated Aug 1 from a source reading 「予約開始は3か月前の月初」, which is Jul 1 for an October stay. Read the month back off the table, treat "when does it CLOSE" as the fact the day turns on, and re-derive every date the guide COMPUTES instead of copying the rule's conclusion. | open |
| 2026-08-28 | yamagata | [critic] | row 3 (provenance) / process | **PROMOTE — the critic-truth handoff is structurally unsatisfiable across a re-attempt, and four consecutive attempts have now failed the same closed gate for the same reason** — a failed critic attempt keeps its guide edits in the tree, `stages.critic.baseline` is pinned ONCE at the pre-critic commit and never re-pinned (`if (baseline && !st.baseline)`), and each new attempt is prepared blind with no git history, so it can declare `previousValue` only for leaves IT changed while every earlier attempt's leaf is undeclarable forever. No number of retries closes it; the only in-stage mitigation is to shape edits as NEW leaves, whose `previousValue` is `null` against any baseline. Fix in the control plane, one of: hand the stage the baseline values for already-changed leaves, reset the workspace to the baseline when an attempt fails, or re-pin the baseline to the tree each attempt actually receives. Same shape one layer down: a blind stage that can create a file but has no delete-capable tool guarantees a scope failure. | open |
