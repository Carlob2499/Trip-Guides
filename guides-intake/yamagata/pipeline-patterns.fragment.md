| 2026-08-28 | yamagata | [critic] | row 6 (anchor coverage) | **an operator's service NOTES are the anchor's most decision-relevant content, and they sit beside the timetable rather than in it** — the trip's anchor transfer shipped with times but without the two ※/☆ notes on the same page: that one departure does not connect to the northbound Shinkansen, and that the last bus is expected to be crowded with no extra service added. The guide's own "aim for a mid-afternoon departure" then named precisely the non-connecting bus. When the anchor is a scheduled service, read the notes column and carry it: "no extra bus is added" is what decides a departure for a party of eight, and no amount of timetable arithmetic recovers it. | open |
| 2026-08-28 | yamagata | [critic] | row 3 (provenance) | **two seasonal timetables read as a cross-pass source conflict, and the guide shipped the confusion as traveler-facing uncertainty** — Pass A and Pass B fetched the same operator's winter table and its April-October table, found every departure 5-7 minutes apart, recorded a formal `disagreements[]` entry, and told the reader to "treat exact times as approximate". Before opening a disagreement investigation on two same-domain pages, check for a season or validity label on each table: a schedule that differs by a constant small offset across every row is a seasonal variant, not a contradiction. | open |
| 2026-08-28 | yamagata | [critic] | row 9 (party fit) | **the claim that answers a BINDING constraint is the one most likely to be cited to a page that never made it** — a guide whose intake bound mobility as material shipped "the best-documented step-free access of any Yamagata city sight" on two surfaces, cited to the venue's official site, which publishes nothing about accessibility on that page or its access subpage. The accessibility data existed on the prefecture's tourism site all along. A constraint-bound fact deserves the fetch-and-quote treatment reserved for prices, because a superlative about documentation is exactly the sentence a model can write without reading anything. | open |
| 2026-08-28 | yamagata | [critic] | row 3 (provenance) | **RECURRENCE (2nd run, uruguay 2026-08-23) of "the aggregator law gets applied per-claim-type, not per-venue"** — the venue that decided a group-of-eight meal carried `source_url` pointing at a review site's bare homepage, behind a seat-count claim no official page states. Same run correctly refused aggregator sourcing elsewhere. Promotion candidate: a deterministic check that no `source_url` is a bare origin with an empty path would have caught this one mechanically, since a homepage can never be a page-specific citation. | open |
| 2026-08-28 | yamagata | [critic] | row 7 (4-question venue rule) | **`book: "call"` ships without a `phone` and nothing notices** — four of seven venue items instructed the reader to call ahead and gave no number, one of them echoed into a day card. All four numbers were on pages the pass could reach, and one venue also shipped "hours aren't posted" when the prefecture's tourism portal publishes them seasonally. Candidate gate: `book == "call"` with no `phone` is a mechanical readiness failure, not a judgement call. | open |
| 2026-08-28 | yamagata | [critic] | row 5 (itinerary integrity) | **RECURRENCE (2nd run, uruguay 2026-08-23) of "one budget `days` field cannot serve both nights and days"** — a 4-day, 3-night trip put city lodging on `basis: "day"` with `days: 4`, billing four city-hotel nights when two of the three nights were in the city and the third was a separately-priced ryokan. Recurring across two consecutive runs on the same field for the same reason: this is now a defect in the pipeline's law rather than a pattern, and the rule "lodging is `basis: trip` whenever nights differ from days" belongs in block-types.md's budget note or in a readiness check. | open |
| 2026-08-28 | yamagata | [critic] | tone (vibe lens) | **the research environment's own failures get narrated to the traveler** — "bot-gated against automated fetch" appeared in three traveler-facing bodies, describing a scraper's HTTP status to someone deciding how to buy a train ticket. The voice gate's banned list catches "this research" and "our pass" but not infrastructure vocabulary, which arrives by the same route. The honest gap survives the edit intact: the ⚠ and the confirm-at-booking instruction say everything the traveler needs, and the source-access record belongs in the Sources section and the ledger. | open |
| 2026-08-28 | yamagata | [critic] | row 6 (anchor coverage) | **a fix that identifies a seasonal source can still cite the wrong season's page** — the correction that established "the operator runs two seasonal timetables and Apr 1–Oct 24 is ours" then set the transit section's `source_url` to the winter page, which publishes only the Dec–Mar table and says of itself that its information is out of date. It also described both tables as sitting on one page and differing by 5-7 minutes, when they are two pages and the trip's table adds three services the winter one doesn't run — so the traveller-facing "miss a bus, wait 1.5-2.5h" was read off the wrong season. When a source splits by validity range, the citation is the page whose range CONTAINS the trip dates, and any derived figure (gaps, first/last, counts) is re-derived from that page, not the one that was read first. | open |
| 2026-08-28 | yamagata | [critic] | row 3 (provenance) | **RECURRENCE (2nd instance this run, after the Bunshokan finding) of "a section-level `source_url` is treated as covering every sentence in its body"** — a divergences card asserted that a JR station rejects IC cards, cited to a bus operator's fare table that documents only the bus, while two other surfaces of the same guide carried that half as ⚠ unconfirmed; and a health panel asserted 2026 measles activity in Japan on a citation that is a pharmacy how-to mentioning neither measles nor MMR. Twice in one run, so it is the section source, not the individual claim, that needs the check: each distinct claim in a body owes either its own citation or its own ⚠, and the highest-confidence block type (divergences) is the worst place to inherit one. | open |
| 2026-08-28 | yamagata | [critic] | row 3 (provenance) | **the `facts.json` money registry gets used for the two facts a pass thinks of as "the figures" and skipped for the other eight** — this guide registered the bus fare and the temple admission, then typed a Shinkansen fare, a train fare, two restaurant minimums, a museum price, a taxi range and a ryokan rate straight into prose across six files, several of them appearing twice. SKILL.md is unambiguous that perishable money facts go in the registry. The registry earns its keep exactly where the duplication is, so the rule to apply is per-figure and mechanical, not "the important ones". | open |
