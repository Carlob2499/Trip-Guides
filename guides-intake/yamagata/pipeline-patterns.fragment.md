| 2026-08-28 | yamagata | [critic] | row 6 (anchor coverage) | **an operator's service NOTES are the anchor's most decision-relevant content, and they sit beside the timetable rather than in it** — the trip's anchor transfer shipped with times but without the two ※/☆ notes on the same page: that one departure does not connect to the northbound Shinkansen, and that the last bus is expected to be crowded with no extra service added. The guide's own "aim for a mid-afternoon departure" then named precisely the non-connecting bus. When the anchor is a scheduled service, read the notes column and carry it: "no extra bus is added" is what decides a departure for a party of eight, and no amount of timetable arithmetic recovers it. | open |
| 2026-08-28 | yamagata | [critic] | row 3 (provenance) | **a source that splits by VALIDITY RANGE produces both a phantom conflict and a wrong citation, in that order** — Pass A and Pass B fetched the same operator's winter table and its April–October table, read the constant offset as a cross-pass disagreement, and shipped "treat exact times as approximate"; the critic fix then named the right season and cited the winter page anyway, which says of itself that its information is out of date. Two rules, one check: before opening a disagreement investigation on two same-domain pages, look for a season or validity label on each table; and cite the page whose range CONTAINS the trip dates, re-deriving every figure (gaps, first/last, counts) from that page rather than the one read first. | open |
| 2026-08-28 | yamagata | [critic] | row 9 (party fit) | **the claim that answers a BINDING constraint is the one most likely to be cited to a page that never made it** — a guide whose intake bound mobility as material shipped "the best-documented step-free access of any Yamagata city sight" on two surfaces, cited to the venue's official site, which publishes nothing about accessibility on that page or its access subpage. The accessibility data existed on the prefecture's tourism site all along. A constraint-bound fact deserves the fetch-and-quote treatment reserved for prices, because a superlative about documentation is exactly the sentence a model can write without reading anything. | open |
| 2026-08-28 | yamagata | [critic] | row 3 (provenance) | **RECURRENCE (uruguay 2026-08-23, then four times inside this one run) of "the citation cannot support the claim, and nothing notices"** — one venue's `source_url` was a review site's bare homepage; a divergences card asserted a JR station's IC-card policy from a bus operator's fare table; a health panel asserted 2026 measles activity from a pharmacy how-to; a temple's fee page stood in for its opening hours. The section-level `source_url` is the vector every time: each distinct claim in a body owes either its own citation or its own ⚠, and the highest-confidence block types (divergences, safety) are the worst places to inherit one. Promotion candidate, mechanical half: no `source_url` may be a bare origin with an empty path. | open |
| 2026-08-28 | yamagata | [critic] | row 7 (4-question venue rule) | **`book: "call"` ships without a `phone` and nothing notices** — four of seven venue items instructed the reader to call ahead and gave no number, one of them echoed into a day card. All four numbers were on pages the pass could reach, and one venue also shipped "hours aren't posted" when the prefecture's tourism portal publishes them seasonally. Candidate gate: `book == "call"` with no `phone` is a mechanical readiness failure, not a judgement call. | open |
| 2026-08-28 | yamagata | [critic] | row 5 (itinerary integrity) | **RECURRENCE (2nd run, uruguay 2026-08-23) of "one budget `days` field cannot serve both nights and days"** — a 4-day, 3-night trip put city lodging on `basis: "day"` with `days: 4`, billing four city-hotel nights when two of the three nights were in the city and the third was a separately-priced ryokan. Recurring across two consecutive runs on the same field for the same reason: this is now a defect in the pipeline's law rather than a pattern, and the rule "lodging is `basis: trip` whenever nights differ from days" belongs in block-types.md's budget note or in a readiness check. | open |
| 2026-08-28 | yamagata | [critic] | tone (vibe lens) | **the research environment's own failures get narrated to the traveler, and the clean-up grep misses the guide-level fields** — "bot-gated against automated fetch" appeared in three traveler-facing bodies; a later pass grepped for it, recorded the sweep, and still left a fourth occurrence in `_guide.json`'s `verified` stamp, which renders as the masthead pill. The voice gate's banned list catches "this research" and "our pass" but not infrastructure vocabulary, and it reads `body`/`why`/`crowd_tip`/`intro` — not the guide-level stamp. Sweep the stamp like any other surface; the ⚠ and the confirm-at-booking instruction already say everything the traveler needs. | open |
| 2026-08-28 | yamagata | [critic] | row 3 (provenance) | **the `facts.json` money registry gets used for the two facts a pass thinks of as "the figures" and skipped for the other eight** — this guide registered the bus fare and the temple admission, then typed a Shinkansen fare, a train fare, two restaurant minimums, a museum price, a taxi range and a ryokan rate straight into prose across six files, several of them appearing twice. SKILL.md is unambiguous that perishable money facts go in the registry. The registry earns its keep exactly where the duplication is, so the rule to apply is per-figure and mechanical, not "the important ones". | open |
| 2026-08-28 | yamagata | [critic] | row 7 (4-question venue rule) | **CLOCK facts get a fraction of the scrutiny MONEY facts get, and a seasonal hours table can have a GAP the trip falls into** — three passes and two critics audited every price to a quoted primary source, while the trip's two load-bearing times shipped uncited: a 1,015-step temple day built on "the 8:00 opening" (cited to the fee page; the temple publishes 4月-9月 and 12月-3月 brackets and **nothing for October**, and no closing time was stated anywhere), and an onsen arrival timed to a gas-lamp window its citation gives as "at dusk". An absent bracket looks exactly like a present one once the figure is copied out — so read the month back off the table, and treat "when does it CLOSE" as the fact the day actually turns on. | open |
| 2026-08-28 | yamagata | [critic] | row 3 (provenance) / process | **the critic-truth handoff is unrecoverable across a re-attempt, because the retained edits and the pinned pre-critic baseline are visible to different parties** — a failed critic attempt keeps its guide edits in the tree, the baseline stays pinned at the reconcile commit, and the next attempt is prepared blind with no git history, so it can declare `previousValue` only for leaves IT changed; every earlier attempt's leaf is structurally undeclarable and fails closed forever. Same shape one layer down: this stage could create a scratch file at the repo root and had no tool able to delete it, guaranteeing a `touched forbidden path` scope failure. A blind stage must be handed either the baseline values for already-changed leaves, or a workspace reset to the baseline — and a delete-capable tool. | open |
