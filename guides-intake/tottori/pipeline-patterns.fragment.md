| 2026-08-26 | tottori | [critic] | row 3 (provenance) | **the citations nobody doubted are the ones that were wrong** — a critic pass fixed the four citations it had reason to suspect and its audit table marked the rest `supports`; a second pass re-fetched those same "supporting" rows and four carried figures the page never publishes (a ¥800 rental price, a 116-seat count, a "free parking" claim the official source actively contradicts, a dawn clock time). An audit row written from the fact you expect, rather than from the text on the page, is not an audit. Every later pass must RE-SAMPLE rows a previous audit passed, not only the facts it changed. | open |
| 2026-08-26 | tottori | [critic] | row 2 (no fabrication) | **a venue whose NAME borrows a famous place inherits that place's coordinates** — "San'in Matsushima" cruise, in Tottori, shipped with the lat/lng and place_id of Matsushima in Miyagi Prefecture, ~600 km away and on the other side of Honshu, surviving Pass A, Pass B, reconcile and a full critic pass. Nothing compares an item's pin against the guide's own geography. Cheap deterministic gate: fail any item coordinate falling outside the first `map` section's `center ± span` (lng) / `± span × 0.6` (lat), or beyond a sane radius of the day cards' towns. | open |
| 2026-08-26 | tottori | [critic] | row 7 (4-question venue rule) | **"how to get there" goes missing on the sight everyone already knows** — second consecutive critic run on the same guide: after the previous pass added car-free access to the Nageiredo climb, the rank-1 Sand Dunes still had a fee, a dwell time, crowd timing and a novel walking route, and no way for a car-free party of eight to reach them. Famous sights get fee/hours/crowd depth because that is what sources publish; access is what a no-rental-car party actually needs. Recurrence within one guide — promotion candidate: make "how from base" the FIRST of the four questions checked, on marquee sights especially. | open |
| 2026-08-26 | tottori | [critic] | common sense | **a larger-vehicle fallback priced from the operator's headline tariff** — the 9-seat jumbo taxi, the reserved answer for a party of eight after the last bus, was costed at the regular car's meter (¥740 + ¥90/279m against the jumbo's own ¥840 + ¥100/179m), understating the fallback by ~60% on the run it exists for. A bigger vehicle almost always has its own row on the same fare page. Read the row for the vehicle you are booking, the same discipline as reading the column for the bus you are actually catching. | open |
| 2026-08-26 | tottori | [critic] | row 10 (honest gaps) | **the same claim ships flagged on one tab and clean on another** — twice in one guide: 7-Eleven ATM guidance shipped ⚠ "rests on a search summary" in Money and as plain detailed fact in Plan, and a ramen price shipped `≈` while the hours from the IDENTICAL page shipped ⚠ "no qualifying primary source". A reader comparing two tabs learns the guide doesn't know what it thinks. Confidence markers need a sweep BY CLAIM and BY SOURCE across the whole guide, not per section — one page's tier decides every field taken from it. | open |
| 2026-08-26 | tottori | [critic] | meals & energy | **the #2 ranked priority can have zero coverage on a night the guide sleeps elsewhere** — food ranked second in the intake and both shipped venues sat in the day-2 town, leaving the arrival night in the base city with no meal at all for eight people. The candidates table proved the slot HAD been researched and rejected with no survivor; nothing said so. A ranked priority with a rejected-and-empty slot owes an explicit statement of the gap plus the actionable fallback, not silence that reads as "nothing worth eating here". | open |
| 2026-08-26 | tottori | [critic] | row 1 (schema valid) | **a derived provenance marker double-counts when it is also typed into the value, and then leaks into every section that references the fact** — `facts.json` carried `value: "≈¥159..."` alongside `state: "approx"`, so the render would print two markers, and because the derived `≈ approx.` pill is interpolated BEFORE schema validation, every panel referencing that fact silently inherited a `provenance:"strict"` obligation to carry `verified_on`. The build failure surfaced two tabs away from the one-character cause. Never type `≈` into `value`; when a strict-mode panel trips the ≈ gate with no visible ≈, look for a `{{fact:}}` token first. | open |
