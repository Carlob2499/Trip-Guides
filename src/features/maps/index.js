/** maps — the Google Maps upgrade path over the default OSM embed.
    Config-gated (PUBLIC_GMAPS_KEY): without a key this module is inert and
    the keyless OpenStreetMap iframe stands. Importing boots the upgrade. */
import "./ui/gmaps-render.js";
