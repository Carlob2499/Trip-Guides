/** exports — the guide, portable: iCal day events, GPX waypoints, summaries.
    Pure build-time builders (consumed by the .ics/.gpx endpoints, the OG
    pipeline and the hub); no client code. */
export * from "./model/exports";
