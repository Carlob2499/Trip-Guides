# Waypoint Production Field-Hardening Ledger

| Field | Value |
|---|---|
| Campaign branch | `external-ai/production-field-hardening` |
| Starting `origin/main` | `c3ab203ae9b94c747fa8e08051dd2f18c28069aa` |
| Baseline verified | 2026-08-26 EDT |
| Production URL | `https://carlob2499.github.io/Trip-Guides/` |
| Main CI/deploy posture | Current `c3ab203` Push on main, Pages deploy, Worker deploy, and Accessibility workflows succeeded. |
| Open PRs | None at baseline. |
| Protected surfaces | V1 remains default; all V2 research/cutover authority, DS1/design authority, factual guide content, Firebase rules, and hosting architecture remain untouched. |

## Finding register

| ID | Status | Primary class | Reproduction | Smallest owner | Files changed | Regression evidence | Remaining verification |
|---|---|---|---|---|---|---|---|
| FH-001 | Fixed; verification continuing | Accessibility / keyboard focus | On deployed Denmark: press Tab from load (focus reaches `Skip to content`), press Enter. URL updates to `#content`, but `document.activeElement` remains outside content because `GuideLayout.astro` renders `<main id="content">` without `tabindex`. The field-harness keyboard check reproduced this in an isolated browser context. | `src/layouts/GuideLayout.astro` | `src/layouts/GuideLayout.astro`; `tests/visual/a11y.spec.ts` | Added a focused established-suite test; it failed pre-fix with `#content` inactive, then passed after the one-line programmatic focusability repair. `npm run build` passed. | Run the full suite and production post-deploy Skip-link retest after the review branch is published; do not claim a deployed fix before it is live. |

## Deployed black-box observations — 2026-08-26 EDT

| Surface | Safe traveler exercise | Observed result | Classification |
|---|---|---|---|
| Landing / guide opening | Opened production landing, then Denmark through its public card. | Hub and guide rendered with working Skip, guide card, field tabs, and no blank state. | No defect observed. |
| Search / recovery | Opened Search, then pressed Escape. | Search overlay opened with visible section results and closed via Escape. | No defect observed. |
| SOS / recovery | Opened SOS, inspected emergency controls, then pressed Escape without invoking telephone links. | Emergency sheet displayed a visible advisory and Denmark emergency numbers; Escape restored the guide. | No defect observed. |
| Itinerary | Switched to Days through the public tab. | Nine-day timeline, day jump controls, map handoffs, day kit, and print controls rendered; no stale or blocked loading state observed. | No defect observed. |
| Direct deep link / history | Opened `guides/denmark/#days` directly, then used browser Back. | Direct URL restored the Days surface with its itinerary controls. Back returned to the prior Transit tab state without a blank page, leaving written offline fallback text visible under the map area. | No defect observed. |
| Tools / shared-state disclosure | Opened Tools without submitting reminders, people, or expenses. | Reminder and Trip Split surfaces rendered. The product explicitly warns that the shared room is visible to anyone with the guide link and explicitly labels current local/offline state; no misleading write-success claim was observed. | No defect observed. |
| Corrupted local reminder state | Set this browser profile’s `tg-toolsrem-denmark` to parseable primitive `true`, reloaded `#tools`, then invoked one existing reminder checkbox. | Tools rendered; the first real checkbox event normalized the primitive to a valid object with the reminder id. The test key was then removed. No shared-room request was made. | Recovery verified; no defect observed. |
| Firebase disposable-room join | Opened `https://carlob2499.github.io/Trip-Guides/guides/denmark/#room=FIELDTEST-20260826T061116Z`; waited for async initialization; submitted no reminder, person, expense, or checklist mutation. The public `#tgConfig` contained the committed Denmark `roomId`, while the fragment supplied the unique field-test override. | Both the shared reminder surface and Trip Split settled on their truthful device-local/offline labels. Browser resource timing showed the Firebase bundle and anonymous-auth sign-up request; the browser console contained no exposed application error. No field-test room write was attempted because join did not reach a live state. | **Environment/service availability finding; no source defect confirmed.** The feature’s documented fail-safe degraded state activated, but real SDK/auth/rules/network/ack/two-client convergence remains unproven and unavailable in this browser session. |
| Worker health | Read-only `GET https://waypoint-intake.carlob24r.workers.dev/health`, after reading the deployed endpoint configuration and Worker router contract. | `{"ok":true,"repo":"Carlob2499/Trip-Guides","rateLimit":"configured","ownerEndpoints":"configured"}`. No intake, owner endpoint, rate-limit, or data-changing request was made. | Worker public configuration posture verified; no defect observed. |
| Worker public validation | From the actual GitHub Pages production origin, sent exactly one documented malformed public request: `POST /intake` with `{}`. | Returned `400` with `{"error":"country: Invalid input: expected string, received undefined"}`. The request failed schema validation before rate-limit or GitHub issue side effects; no valid intake, owner route, brute-force attempt, or stress action occurred. | Public safe failure verified; no defect observed. |
| Responsive public guides | System-Chromium production observer loaded Denmark Tools and Korea Plan at `320×740`, `390×844`, `768×1024`, and `1280×800`, retaining screenshots under `/home/ubuntu/field-hardening-mobile/`. It measured document width and visually reviewed both 320-pixel captures. | All eight loads returned HTTP 200; each viewport measured `0px` document-level horizontal overflow. Denmark’s active Tools tab remained visible in the horizontally scrollable field navigation; Korea’s long content, Korean address control, and guide navigation rendered without a page-width blowout. The observer reported small native checkbox boxes in Korea, but their adjacent full-row labels are the intended associated activation surface; no missed or unreachable activation was reproduced. | Mobile/tablet/desktop layout pass: no defect observed. |
| Real PWA offline / reconnect | In a fresh isolated 390px browser context, opened `guides/denmark/#days` online, waited for service-worker readiness, set browser offline, reloaded the same public route, then restored connectivity and reloaded again. No writes, reminders, expenses, or shared state were touched. | Online load returned 200 with `serviceWorker.state = activated`. Offline reload returned 200, preserved title, Denmark content, and Days content at the same route; visual screenshot showed a usable guide shell and local route controls. Reconnect reload returned 200 with Days content. | Production PWA reading/navigation cache path verified. This is not a shared-write replay, Firebase, or two-client convergence claim. |
| Keyboard recovery / reduced motion | In an isolated public Denmark session, tabbed to the initial control, activated Skip to content, opened Search and SOS separately, then dismissed each with Escape. In a separate 390px Korea session with browser reduced motion enabled, observed the hero-media and document motion state. | First keyboard focus was `Skip to content` (`href="#content"`); activation set `#content`. Escape after Search returned focus to the Search control and no visible dialog remained; Escape after SOS returned focus to the SOS control. With reduced motion, the Korea video was present but paused with autoplay disabled and the document animation duration was `0s`. | Keyboard/recovery/reduced-motion pass: no defect observed. |
| Production performance baseline | Three fresh 390px, service-worker-blocked public loads each of `denmark/#days` and `korea/`, with browser navigation, paint, and resource-transfer timing retained under `/home/ubuntu/field-hardening-performance/`. | Denmark median: DOMContentLoaded `407ms`, load `626ms`, FCP `156ms`, `484,405B` across 31 resources. Korea median: DOMContentLoaded `490ms`, load `4,490ms`, FCP `144ms`, `634,761B` across 35 resources. All six responses were 200 with correct titles. Korea’s complete load was longer, but first contentful paint and visible traveler content were fast; no user-visible delay or owner-level regression was reproduced. | Baseline captured; no performance change justified. |
| Local performance-budget gate | Ran `npm run check:offline && npm run check:perf` after FH-001. Offline contract passed (4/4); performance budget failed only because `dist/progress/index.html` first-paint JS measured `219KB` versus its `200KB` ceiling. Built and ran the identical gate in a detached `c3ab203` baseline worktree using the same installed dependencies; it failed with the same page, byte count, chunks, and largest asset. | **Baseline/environment dependency-resolution finding, not FH-001.** Current branch source diff contains only guide-main focusability plus its test; baseline replication isolates the failure from this campaign change. No unrelated progress/performance/dependency/architecture modification is authorized. | Preserve as a disclosed local gate limitation; revalidate in repository CI’s locked dependency environment after PR creation. |

## Explicitly protected live-environment gaps

| Gap | Classification | Campaign handling |
|---|---|---|
| Actual Firebase SDK/auth/deployed rules/real-network/two-client convergence | Environment/external dependency; active PR #100 ownership history | Test only through a new disposable `FIELDTEST-<timestamp>` room if the deployed UI supports safe creation. Never touch existing traveler data, weaken rules, or manufacture a rejection. |
| Firebase disposable-room live join | Environment/service availability finding; no smallest source owner established | A fresh `FIELDTEST-20260826T061116Z` room failed closed to truthful local-only UI after the production app initiated Firebase loading and anonymous auth. Preserve the test URL/observation; do not manufacture a direct REST/owner/rules test or modify Firebase configuration/rules during this campaign. |
| Frozen V2 validation/cutover sequence | Protected research-validation surface | Observe only; document any field finding without modifying Run A, Run B, V01/V02/V03/V05/V07, selector, publication, or research doctrine. |
