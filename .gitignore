node_modules/
dist/
.astro/
.DS_Store
*.zip
CLAUDE.md
.env
.claude/skills/waypoint-guide-author-workspace/
.claude/worktrees/
test-results/
playwright-report/
coverage/
# W2 feedback-export: the agent's working file carries freeform (private) — never committed.
feedback-export.working.json
# W3 run ledger: the agent's per-run report is posted to an issue, never committed to the guide PR.
run-report.md
# V6 revise pipeline: CI-run working files — signals feed an issue, context/changes feed agents.
revision-signals.json
revision-context/
revision-changes.txt
change.txt
forks.json
validate-errors.txt
docs/mockups/living-atlas.html
docs/mockups/.cache/

reports/
.stryker-tmp/

# impeccable skill scratch output — tool artifacts, not project docs
.impeccable/

# claude-codex-watcher.yml: Codex's work-order text for the fix agent, written into
# pr-workspace/ at run time — defense in depth on top of the workflow's own explicit rsync
# --exclude (Codex finding P1, PR #78) so this scratch file can never reach a real commit.
.codex-work-order.md
