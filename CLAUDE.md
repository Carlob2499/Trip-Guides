# Waypoint Travel Guides — Project Instructions

Full architecture, file structure, and stack detail: see Project_State.md
(Read on demand; not duplicated here to keep this file lean).

Project-specific rules for this repo. These stack on top of the global
CLAUDE.md (universal accuracy rules, ~/.claude/CLAUDE.md — auto-loads
separately, not duplicated here) — both apply when working here.

---

## What Waypoint Is (and Is Not)

Waypoint is a platform for *verified, personalized* travel guides. Every
rule here protects two things at once: factual accuracy AND genuine fit
to a specific traveler. A guide that is accurate but generic has failed.
A guide that is personal but unverified has failed. Both are required.

We are NOT an autogenerated itinerary tool. We never produce a guide from
training data alone and call it done.

### The four properties
A Waypoint guide is distinguished by four properties. If any is missing,
the guide is not done:

1. **Verified** — every perishable fact traced to a primary source, with
   a verification date.
2. **Personal** — structured around one traveler's actual ranked
   priorities, not a generic "things to do in X" list.
3. **Actionable** — every recommendation answers how to get there, when
   it fits, and whether to book. No fact sits without its logistics.
4. **Honest** — gaps are stated, not filled. An admitted blank is a
   feature, not a failure.

### The autogeneration test
Before delivering any guide content, ask: "Could a generic AI have
written this from training data alone, without research and without
knowing who this traveler is?" If yes, it does not meet the bar — stop
and either verify it or personalize it.

---

## Building a New Guide — Order of Operations

A new guide is NEVER started by writing content. The sequence is fixed:

1. **Intake first.** Complete NEW_GUIDE_INTAKE.md with the traveler. No
   research begins until priorities are ranked and dates are set.
2. **Spec from intake.** Derive which section types the guide needs from
   the priority ranking. Nightlife ranked last → no nightlife section.
3. **Research per the spec.** Verify every fact against a primary source
   (global rules 1–13). Depth on the top 2–3 priorities; light touch on
   the rest. Gather only what the priorities call for.
4. **Write with sources attached.** Every perishable fact carries its
   source and verification date as written, not bolted on after.
5. **Run the autogeneration test** on the finished draft. Anything
   generic or unverified is cut or fixed.
6. **Save the intake doc with the guide** so future updates know the
   original priorities and who the guide was for.

---

## Content Standards (enforced on every guide)

### Every restaurant/venue entry answers four questions
1. **Where?** Exact address (local + romanized where relevant).
2. **How do I get there from base?** Transit route + approximate time.
3. **When in the trip does it fit?** Best day, and why.
4. **Do I need to book?** Walk-in / reserve online / call ahead.

### Price and confidence flags
- `≈` — found the official source; figure is approximately this. Verify
  before paying.
- `⚠` — hours or details could not be confirmed online. Check before
  going.
- A missing price is honest. A guessed price wearing a `≈` is not.

### Verification stamps
Each guide carries a guide-level "Checked [date]" stamp. Individual
sections with faster-aging facts (restaurant hours, event rosters) should
carry their own per-section verified date where practical.

### Photo validation
Every `img.file` in a `sights` section must be an exact Wikimedia Commons
`File:` page filename. Confirm the page exists before adding. If unsure,
omit — never guess.

### Prose body tag allowlist
`<p> <b> <i> <a> <ul> <li> <ol>` — the complete set. Any other HTML in a
prose body (`<details>`, `<table>`, `<figure>`, `<br/>` as layout) is a
signal to add a typed section instead.

---

## Architectural Guardrails

- **Shared components affect every guide.** Styling, layout, and block
  components are global. A change to any component changes all guides at
  once. Only JSON data files are per-country.
- **Read before editing.** Always read the target JSON before changing
  it. Never regenerate a file from memory.
- **Verify the build.** `npm run build` must pass with zero schema
  errors. Confirm changed paths in compiled `dist/` output, not just
  source, before pushing.
- **Base-path hrefs are explicit.** Every internal nav href starting with
  `/` must be prefixed with `import.meta.env.BASE_URL`. Astro does not
  auto-rewrite template-literal hrefs.
- **New facts get a verification date on write**, never bolted on later.

---

## Gotchas

- This repo is OneDrive-synced: `astro dev`'s HMR can serve STALE CSS
  after edits. Verify CSS changes via `astro preview` (rebuilds + serves
  `dist/`), not `astro dev`.

---

## Operational Habits (save tokens on every session)

- **Large files:** korea.json (~1883 lines), denmark.json (~903), any
  guide over ~400 lines — Grep the target first and Read with
  offset/limit. Never full-file Read a large guide for a targeted edit.
- **New client behavior goes in `src/scripts/`**, never back inline into
  `GuideLayout.astro` (kept lean at ~388 lines — do not let it re-bloat).
- **For objective/greppable questions** (dead selectors, duplicate props,
  magic numbers, fact diffs) use a local grep/script — cheaper and more
  precise than an Explore agent. Reserve agents for genuinely open-ended
  synthesis.
- **If `guide.css` grows past ~800 lines**, split by feature (e.g.
  `weather.css`, `split.css`) as `base.css` was split — do not let one
  file absorb everything. Not before the threshold.
