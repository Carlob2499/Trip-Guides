# D7 implementation — progress log (breadcrumbs for a resumed session)

Branch: `claude/waypoint-design-routing-rex0vr` (started from frozen SHA `b37fe92`).
Authority order: `FABLE5_IMPLEMENTATION_PROMPT.md` in this folder. Read this file first on resume,
then `git log --oneline` and `git status` on the branch. WIP commits may be red; the log says
what is finished and what is mid-flight.

## Architecture decisions taken (do not re-decide)

- Five destinations are regions of ONE guide page (`src/layouts/GuideLayout.astro`):
  `#dest-trip · #dest-itinerary · #dest-map · #dest-guide · #dest-split`. Router =
  `src/scripts/guide-ui.js` (`showDest`, `goToHash`, `data-dest-nav`, `data-dest-go`).
- Global chrome = `src/components/AppChrome.astro` + `src/styles/chrome.css`: topbar (Atlas link,
  desktop destination row, Search, SOS mount `[data-sos-mount]`, share, theme), expanded Search
  field (`.searchbar`, folds on `body.chrome-scrolled`), five-slot bottom bar (yields on
  `body.chrome-yield`).
- Masthead (cover) lives INSIDE the Guide destination (`GuideDestination.astro`); Trip has its own
  restrained hero. Guide = overview cards → one chapter at a time (`data-chapter-panel`,
  `data-chapter-go`), desktop chapter index, spatial chapters get a map aside.
- `src/features/trip/` absorbs trip-tools (reminders, closures) + trip-kit (arrival, book-by,
  speak, entry-select, packing). `#tripData` = canonical days (replaces `#storyDays`).
  `ui/trip.js` paints phase + Now/Next.
- `src/features/search/` owns the index (moved from atlas) + ranking + overlay. Per-guide index
  is embedded as `#searchIndex`; cross-guide index stays `dist/data/search-index.json`.
- Schema additions (`src/content.config.ts`): `module` facet (D6-53 relation layer) and day
  `branches` (D6-46). Validation in superRefine.
- Retired: trip-tools, trip-kit, ToolsScreen, TripKit, tools-reminders.js (→ readiness-ticks.js),
  section-flight.js, onboard.js. Still to retire: guide-rail, palette, mobile-nav rank/swipe/
  resume/botbar/day-scrub, story-mode, spine, panel reorder, Reminders + reminders feature,
  mobile-nav.css, story-mode.css, tools.css, palette.css, planner.css.

- `src/lib/guide-view.ts` = the ONE derivation of everything the destinations receive;
  GuideLayout and the gallery both call `deriveGuideView`.
- Map embeds are lazy: `iframe[data-src]` inside a destination is promoted by `showDest`
  (guide-ui.js) when that destination first shows; the gallery promotes all of them itself.
- Search ranking is token-aware (`queryTokens`), strict-then-relaxed, deterministic.

## Status (2026-09-04) — program complete, PR open

- [x] Slices A–M implemented; `design-system.md §12` is the record of what lives where.
- [x] Gates on the PR head: `check:fast`, `npm run check` (coverage thresholds), `check:offline`,
      `check:perf`, `ship:check`, `npm run drift`, Playwright a11y (71) + resilience (14) +
      offline-sync (8).
- [ ] Gallery screenshot baselines: regenerated by CI on this branch
      (`regenerate-gallery-baselines.yml`) — the required gate's baseline step is red until that
      commit lands; never regenerate locally.

## Acceptance matrix evidence

| Surface | Result | Evidence |
|---|---|---|
| Global shell | PASS | AppChrome: five `[data-dest-nav]` slots, no Tools/More, Search + SOS as controls; resilience "every destination fits at 320px" asserts the exact five keys |
| Atlas | PASS | Globe/table unchanged in kind; table search is the global overlay field; stamps are date-derived lifecycle only (no synthetic status) |
| Trip — pre / active / post | PASS | `TripDestination.astro` phases + `trip.js` Now/Next atoms; a11y Now-atom contrast test; no score, no dashboard widgets |
| Trip Feedback | PASS (unchanged) | Learnings survey untouched: Overall/Pace/Food + Plan-vs-Actual + private reflection |
| Itinerary — mobile / desktop | PASS | Day rail (`[data-day-rail]`), no top arrows as primary; workbench divider 30–70%, panes collapsible; Denmark days 11–13 render two labelled branches |
| Map | PASS | OSM iframe removed only on Google `idle`; SDK-failure probe (fake key, script blocked): OSM frame still present, `data-map-provider` unset; sheet peek/half/full |
| Guide | PASS | Cover hero, overview cards, one chapter level, spatial chapters map aside; Korea modules: incheon-airport-transfer (critical) → Thu Jul 9, key-transit-routes, getting-around-tips, etiquette-language, what-staff-say |
| Split | PASS | Recent expenses + Add expense first; who paid · what · how much · split between; rows read "Even · N people" |
| Search | PASS | Overlay from field/compact control/`/`/Ctrl-K; grouped Places/Itinerary/Guide/Other trips; no page, no AI |
| SOS | PASS | `.topbar-sos` quiet control; sheet rows ≥56px, close 44px; numbers are static HTML (tel:112/119/1330/1339 for Korea) |
| Provenance | PASS (unchanged) | `.prov-dot` + popover; resilience opens one at 320px |
| Offline / degraded | PASS | offline-sync suite (8) + resilience "primed guide readable offline"; lazy map frames; `.map-degraded` note when offline |
| Motion | PASS | transform/opacity only (drift-real 0 new); reduced-motion paths in chrome/search/sheet/trip CSS; masthead motion plays only on first Guide reveal |
| Accessibility | PASS | a11y suite: axe scan on hub/guides/gallery light+dark; 44px on nine devices; 16px inputs; text-zoom 200% probe fits at 390px on all five destinations |
| Generalization | PASS | Korea real content; Denmark flexible windows ("morning", "≈") + branched days; no mockup names/dates in content (only schema/module metadata added) |

Adversarial checks 1–10 (ACCEPTANCE_MATRIX): 1 content diff is metadata-only; 2 Denmark days 11–13
inspected; 3 default build has no Google key — OSM path is the tested path; 4 SDK-failure probe
above; 5 reduced motion emulated in every Playwright run; 6 320/375/390/402/412/440/673/744/834/
1024/1280 in a11y + resilience, text zoom probe, keyboard trap test, touch via coarse-pointer
rules; 7 SOS numbers static + offline suite; 8 grep for Story/voting/Trip Kit/Tools/On Track in
`src` returns comments only (recorded in retirement sweep); 9 Search/SOS are `[data-search-open]`
/ `[data-sos-mount]` controls, never `[data-dest-nav]`; 10 no mockup fact entered content.
