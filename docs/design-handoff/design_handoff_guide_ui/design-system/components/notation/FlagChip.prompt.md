`FlagChip` is how Waypoint hedges — typographically, not verbally. Never write "roughly" or "we think" in prose; flag the figure.

```jsx
<span>₩1,400 <FlagChip kind="approx" onClick={openProvenance} /></span>
```
