`Segmented` carries the hub's WORLD | TABLE switch and the theme switcher.

```jsx
<Segmented options={["World","Table"]} value={mode} onChange={setMode} />
```

The Two Doors Rule requires this control to stay permanently visible on mobile: the immersive surface is never the only way in.
