`Button` is the only pressable primitive. `critical` is reserved for SOS — it is one of the four places red ink is spent.

```jsx
<Button>Open this sheet →</Button>
<Button variant="secondary" size="lg">Tools</Button>
<Button variant="critical">SOS</Button>
```
